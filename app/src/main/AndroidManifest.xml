<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="bobrchess.of.by.belaruschess">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@drawable/ic_belarus_chess_icon"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:restoreAnyVersion="true"
        android:roundIcon="@drawable/ic_belarus_chess_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"><!--to check if needed-->
        <activity android:name="bobrchess.of.by.belaruschess.outdated.MainOldActivity" />
        <activity android:name="bobrchess.of.by.belaruschess.view.activity.impl.AuthorizationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="bobrchess.of.by.belaruschess.view.activity.impl.RegistrationActivity" />
        <activity android:name="bobrchess.of.by.belaruschess.view.activity.impl.SearchUserActivity" />
        <activity android:name="bobrchess.of.by.belaruschess.view.activity.impl.SearchTournamentActivity" />
        <activity android:name="bobrchess.of.by.belaruschess.view.activity.impl.TournamentActivity" />
        <activity android:name="bobrchess.of.by.belaruschess.view.activity.impl.TournamentInfoActivity" />
        <activity android:name="bobrchess.of.by.belaruschess.view.activity.impl.GamesListActivity" />
        <activity android:name="bobrchess.of.by.belaruschess.view.activity.impl.GameActivity" />
        <activity android:name="bobrchess.of.by.belaruschess.view.activity.impl.AddTournamentActivity" />

        <!-- Main activity -->
        <activity
            android:name=".view.activity.impl.MainActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="adjustResize">

            <!-- enable the search widget to send searches to SearchableActivity -->
            <meta-data
                android:name="android.app.default_searchable"
                android:value=".SearchableActivity" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Search activity -->
        <activity
            android:name=".view.activity.impl.SearchableActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>

        <receiver android:name=".AlarmReceiver"></receiver>

        <receiver android:name=".BootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".BootNotificationService"
            android:permission="android.permission.BIND_JOB_SERVICE" />

    </application>
</manifest>